---
title: Windows 运行时类型 JavaScript 表示形式
ms.custom: ''
ms.date: 07/29/2020
ms.prod: microsoft-edge
ms.reviewer: ''
ms.suite: ''
ms.technology: windows-integration
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
manager: ''
ms.openlocfilehash: 0ee55b5dd5071b0f0b31656ae164e9801e98d6ac
ms.sourcegitcommit: 29cbe0f464ba0092e025f502833eb9cc3e02ee89
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 08/20/2020
ms.locfileid: "10942049"
---
# <span data-ttu-id="ae12f-102">Windows 运行时类型 JavaScript 表示形式</span><span class="sxs-lookup"><span data-stu-id="ae12f-102">JavaScript representation of Windows Runtime types</span></span>  

[!INCLUDE [deprecation-note](../includes/legacy-edge-note.md)]  

<span data-ttu-id="ae12f-103">下表描述了 JavaScript 表示 Windows 运行时类型的方式。</span><span class="sxs-lookup"><span data-stu-id="ae12f-103">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="ae12f-104">Windows 运行时功能不适用于在运行时运行Internet Explorer。</span><span class="sxs-lookup"><span data-stu-id="ae12f-104">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <span data-ttu-id="ae12f-105">JavaScript 中的 Windows 运行时类型</span><span class="sxs-lookup"><span data-stu-id="ae12f-105">Windows Runtime types in JavaScript</span></span>  

| <span data-ttu-id="ae12f-106">Windows 运行时类型</span><span class="sxs-lookup"><span data-stu-id="ae12f-106">Windows Runtime type</span></span> | <span data-ttu-id="ae12f-107">JavaScript 表示</span><span class="sxs-lookup"><span data-stu-id="ae12f-107">JavaScript representation</span></span> | <span data-ttu-id="ae12f-108">描述</span><span class="sxs-lookup"><span data-stu-id="ae12f-108">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="ae12f-109">Windows 运行时 `Uint8` 是无符号的 8 位整数，以 JavaScript 编号表示。</span><span class="sxs-lookup"><span data-stu-id="ae12f-109">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="ae12f-110">首先将 JavaScript 值转换为 JavaScript 编号，然后该 `ToUint32` 过程随后转换。</span><span class="sxs-lookup"><span data-stu-id="ae12f-110">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="ae12f-111">如果 JavaScript 数值超出 Uint8 范围，则结果将应用模块 2^8。</span><span class="sxs-lookup"><span data-stu-id="ae12f-111">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span>  |  
| `Int32` | `Number` | <span data-ttu-id="ae12f-112">Windows 运行时 `Int32` 是一个有符号的 32 位整数，表示为 JavaScript 编号。</span><span class="sxs-lookup"><span data-stu-id="ae12f-112">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="ae12f-113">首先将 JavaScript 值转换为 JavaScript 编号，然后该 `ToInt32` 过程随后转换。</span><span class="sxs-lookup"><span data-stu-id="ae12f-113">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="ae12f-114">如果 JavaScript 数字值超出项目范围， `Int32` 则结果将应用模块 2^16。</span><span class="sxs-lookup"><span data-stu-id="ae12f-114">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span>  |  
| `Int64` | `Number` | <span data-ttu-id="ae12f-115">Windows 运行时 `Int64` 是一个有符号的 64 位整数，如果位于范围内的 \[2^53， 2^53\]，则用作标准数字表示。</span><span class="sxs-lookup"><span data-stu-id="ae12f-115">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="ae12f-116">如果它位于此范围之外，则此值表示为具有自定义支持存储的数字值，该值维护整数数据的整数 64 位。</span><span class="sxs-lookup"><span data-stu-id="ae12f-116">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="ae12f-117">对这些自定义 Int64 数字值的所有数学运算都会导致值转换为区域 \[-2^53， 2^53\]中的标准数字。</span><span class="sxs-lookup"><span data-stu-id="ae12f-117">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="ae12f-118">如果值在此范围之外，将引发类型错误。</span><span class="sxs-lookup"><span data-stu-id="ae12f-118">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="ae12f-119">此转换过程的例外是，在传递两个表示 64 位值时，根据 `==` `===` `SameValue` 完整的 64 位对参数进行比 `RelationalComparison` 较。</span><span class="sxs-lookup"><span data-stu-id="ae12f-119">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="ae12f-120">如果任一参数是标准 JavaScript 编号，则在比较之前，这些操作还会将该参数转换为 JavaScript 号。</span><span class="sxs-lookup"><span data-stu-id="ae12f-120">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="ae12f-121">如果 JavaScript 值为 Int64 值本身，则直接分配它;否则，将转换应用于 `ToInteger` 值的结果传递给 Windows 运行时。</span><span class="sxs-lookup"><span data-stu-id="ae12f-121">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="ae12f-122">如果类型转换失败，则返回异常。</span><span class="sxs-lookup"><span data-stu-id="ae12f-122">If the type coercion fails, an exception is returned.</span></span>  |  
| `Uint64` | `Number` | <span data-ttu-id="ae12f-123">Windows 运行时 `Uint64` 是无符号的 64 位整数;如果位于范围内的 \[0， 2^53\]，则用标准数字表示。</span><span class="sxs-lookup"><span data-stu-id="ae12f-123">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="ae12f-124">如果它位于此范围之外，则此值表示为具有自定义备份存储的数字，该序列具有不签类整数数据的全部 64 位。</span><span class="sxs-lookup"><span data-stu-id="ae12f-124">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="ae12f-125">对这些自定义 Uint64 值的所有数学运算都会导致值转换为区域 \[0， 2^53\]中的标准数字。</span><span class="sxs-lookup"><span data-stu-id="ae12f-125">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="ae12f-126">如果值在此范围之外，将引发类型错误。</span><span class="sxs-lookup"><span data-stu-id="ae12f-126">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="ae12f-127">此转换过程的例外是，在传递两个 64 位值时，将基于完整 `==` `===` 的 64 位对参数 `SameValue` `RelationalComparison` 进行比较。</span><span class="sxs-lookup"><span data-stu-id="ae12f-127">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="ae12f-128">如果任一参数是标准 JavaScript 编号，则在比较之前，这些操作还会将该参数转换为 JavaScript 号。</span><span class="sxs-lookup"><span data-stu-id="ae12f-128">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="ae12f-129">如果 JavaScript 值为 Uint64 值本身，则直接分配它;否则，将转换应用于该值的结果 `ToInteger` （后跟模块 2^52）会传递到 Windows 运行时。</span><span class="sxs-lookup"><span data-stu-id="ae12f-129">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="ae12f-130">如果类型转换失败，则返回异常。</span><span class="sxs-lookup"><span data-stu-id="ae12f-130">If the type conversion fails, an exception is returned.</span></span>  |  
| `Single` | `Number` | <span data-ttu-id="ae12f-131">Windows 运行时是一个 32 位浮点数，由于将最接近的双精度值选取到单精度值，表示 `Single` 为 JavaScript 编号。</span><span class="sxs-lookup"><span data-stu-id="ae12f-131">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="ae12f-132">JavaScript 值转换为 JavaScript 号，然后进行检查的范围，以确保该值可在单精度 32 位 IEEE 754 浮点数中表示。</span><span class="sxs-lookup"><span data-stu-id="ae12f-132">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="ae12f-133">如果转换或范围检查失败，则返回边距错误。</span><span class="sxs-lookup"><span data-stu-id="ae12f-133">If the conversion or range check fails, a marshaling error is returned.</span></span>  |  
| `Double` | `Number` | <span data-ttu-id="ae12f-134">Windows 运行时 `Double` 是一个 64 位浮点数。</span><span class="sxs-lookup"><span data-stu-id="ae12f-134">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="ae12f-135">用于将 Windows 运行时转换为 `Double` JavaScript 号码。</span><span class="sxs-lookup"><span data-stu-id="ae12f-135">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="ae12f-136">如果转换失败，则返回边距错误。</span><span class="sxs-lookup"><span data-stu-id="ae12f-136">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Boolean` | `Boolean` | <span data-ttu-id="ae12f-137">Windows 运行时 `Boolean` 是 8 位值，其中 0 和 `false` 零以外的任何值都 `true` 表示为 JavaScript。 `Boolean`</span><span class="sxs-lookup"><span data-stu-id="ae12f-137">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="ae12f-138">首先将 JavaScript 值转换为 `Boolean` `ToBoolean` JavaScript。</span><span class="sxs-lookup"><span data-stu-id="ae12f-138">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="ae12f-139">这意味着声明为可以使用 JavaScript 编写的 Windows `void func(BOOL);` 运行时函数 `obj.func("test");` 。</span><span class="sxs-lookup"><span data-stu-id="ae12f-139">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="ae12f-140">使用作为传递形式传递 `BOOL` 的参数调用 Windows 运行时 `TRUE` 函数。</span><span class="sxs-lookup"><span data-stu-id="ae12f-140">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="ae12f-141">如果转换失败，则返回边距错误。</span><span class="sxs-lookup"><span data-stu-id="ae12f-141">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Char16` | `String` | <span data-ttu-id="ae12f-142">Windows 运行时 `Char16` 是 16 位 Unicode 字符，表示为 JavaScript 字符串，该字符包含单个字符。</span><span class="sxs-lookup"><span data-stu-id="ae12f-142">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="ae12f-143">JavaScript 值将转换为 JavaScript 字符串 `ToString` ，然后进行检查，以确保该字符串长度仅一个字符。</span><span class="sxs-lookup"><span data-stu-id="ae12f-143">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="ae12f-144">然后将单个字符作为 `Char16` 值传递。</span><span class="sxs-lookup"><span data-stu-id="ae12f-144">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="ae12f-145">如果转换或长度检查失败，则返回一个边距错误。</span><span class="sxs-lookup"><span data-stu-id="ae12f-145">If the conversion or length check fails, a marshaling error is returned.</span></span>  |  
| `String` | `String` | <span data-ttu-id="ae12f-146">Windows `String` 运行时是一个不可拟的对象 `Char16` 序列。</span><span class="sxs-lookup"><span data-stu-id="ae12f-146">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="ae12f-147">字符串表示为 JavaScript `String` 对象。</span><span class="sxs-lookup"><span data-stu-id="ae12f-147">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="ae12f-148">Windows 运行时字符串对于空字符串 `null` \ ("\) 。</span><span class="sxs-lookup"><span data-stu-id="ae12f-148">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="ae12f-149">和空字符串值将转换为 JavaScript 空字符串。</span><span class="sxs-lookup"><span data-stu-id="ae12f-149">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="ae12f-150">**注意**：当 JavaScript 传递到需要字符串的 Windows 运行时参数时，会得到字符串 `null` 值"null"，而不是将会创建空 `null` 字符串 \ ("\) 。</span><span class="sxs-lookup"><span data-stu-id="ae12f-150">**NOTE**:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="ae12f-151">传递到 Windows 运行时的字符串应始终将其实例化为空字符串。</span><span class="sxs-lookup"><span data-stu-id="ae12f-151">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span>  |  
| `Enumeration` | `Object` | <span data-ttu-id="ae12f-152">Windows 运行时 `Enumeration` 是一个具有关联的命名常量的 32 ) 位整数 \ (有符号或未签名\控件。</span><span class="sxs-lookup"><span data-stu-id="ae12f-152">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="ae12f-153">在 JavaScript 中，枚举表示为一个对象，该对象包含每个命名值的只读字段。</span><span class="sxs-lookup"><span data-stu-id="ae12f-153">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="ae12f-154">枚举值将按前面定义和转换为 JavaScript 数字 `Int32` `UInt32` 。</span><span class="sxs-lookup"><span data-stu-id="ae12f-154">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="ae12f-155">当 JavaScript 值转换为 Windows 运行时枚举时，会将其转换、运行并按之前所述选中的范围。</span><span class="sxs-lookup"><span data-stu-id="ae12f-155">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="ae12f-156">但是，不会对枚举中指定的已定义命名值进行查看结果值。</span><span class="sxs-lookup"><span data-stu-id="ae12f-156">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span>  |  
| `Structure` | `Object` | <span data-ttu-id="ae12f-157">Windows 运行时 `Structure` 是命名数据字段的异类集合。</span><span class="sxs-lookup"><span data-stu-id="ae12f-157">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="ae12f-158">在 JavaScript 中，结构表示为一个 JavaScript 对象，该对象包含在结构中为每个字段包含一个命名属性。</span><span class="sxs-lookup"><span data-stu-id="ae12f-158">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="ae12f-159">如果任何结构值的转换失败，则返回一个重叠错误。</span><span class="sxs-lookup"><span data-stu-id="ae12f-159">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="ae12f-160">将忽略 JavaScript 对象中没有等效项的字段。</span><span class="sxs-lookup"><span data-stu-id="ae12f-160">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="ae12f-161">**注意**：Windows 运行时结构无法在包含关键字的 JavaScript 中 `new` 实例化。</span><span class="sxs-lookup"><span data-stu-id="ae12f-161">**NOTE**:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span>  |  
| `Array` | `Array` | <span data-ttu-id="ae12f-162">Windows 运行时 `Array` 会转换为 JavaScript 数组。</span><span class="sxs-lookup"><span data-stu-id="ae12f-162">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="ae12f-163">但是，Windows 运行时数组不可调整大小，因此不可使用某些 JavaScript 数组操作。</span><span class="sxs-lookup"><span data-stu-id="ae12f-163">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="ae12f-164">转换数组 `[[Class]]` 的内部属性不设置为"数组"，因为 ECMAScript 5 规范不允许此属性。</span><span class="sxs-lookup"><span data-stu-id="ae12f-164">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="ae12f-165">这意味着 `Array.isArray(v)` 返回。 `false`</span><span class="sxs-lookup"><span data-stu-id="ae12f-165">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="ae12f-166">JavaScript 值转换为 Windows 运行时数组，如下所示：如果值 `null` 或 `undefined` ，它将转换为本机 `null` 。</span><span class="sxs-lookup"><span data-stu-id="ae12f-166">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="ae12f-167">如果内部属性 `[[Class]]` 是"数组"，则将其复制到本机数组并传递对此数组的引用。</span><span class="sxs-lookup"><span data-stu-id="ae12f-167">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="ae12f-168">如果是一个转换数组，如前所述，将传递基础本机数组。</span><span class="sxs-lookup"><span data-stu-id="ae12f-168">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="ae12f-169">**注意**：将 JavaScript 数组值传递到 Windows 运行时方法会导致数组的完整副本。</span><span class="sxs-lookup"><span data-stu-id="ae12f-169">**NOTE**:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span>  |  
| <span data-ttu-id="ae12f-170">委有 \ (回调\) </span><span class="sxs-lookup"><span data-stu-id="ae12f-170">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="ae12f-171">Windows 运行时委理是对单个方法的引用。</span><span class="sxs-lookup"><span data-stu-id="ae12f-171">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="ae12f-172">Windows 运行时委委注册以 JavaScript 形式表示，以适当的会话 `Function` 调用。</span><span class="sxs-lookup"><span data-stu-id="ae12f-172">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="ae12f-173">调用 `Function` 后，参数将转换为等效的 Windows 运行时参数类型。</span><span class="sxs-lookup"><span data-stu-id="ae12f-173">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="ae12f-174">如果的 JavaScript 参数少于代理 `in` 参数，则委代调用将失败。</span><span class="sxs-lookup"><span data-stu-id="ae12f-174">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="ae12f-175">如果有多于委g中的参数数量的 JavaScript 参数，则将忽略额外的 `in` JavaScript 参数。</span><span class="sxs-lookup"><span data-stu-id="ae12f-175">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="ae12f-176">在调用委Ggeg 后， `out` 参数将转换为 JavaScript 类型并返回。</span><span class="sxs-lookup"><span data-stu-id="ae12f-176">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="ae12f-177">如果一个本机 JavaScript 函数对象转换为 Windows 运行时委委注册，它将包在对应类型的 Windows 运行时委委集中。</span><span class="sxs-lookup"><span data-stu-id="ae12f-177">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="ae12f-178">调用委Ggeg 时， `in` 参数将转换为 JavaScript 类型。</span><span class="sxs-lookup"><span data-stu-id="ae12f-178">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="ae12f-179">在调用委Gate 之后，返回值将转换 `out` 为委g的参数。</span><span class="sxs-lookup"><span data-stu-id="ae12f-179">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span>  |  
| <span data-ttu-id="ae12f-180">接口</span><span class="sxs-lookup"><span data-stu-id="ae12f-180">Interface</span></span> | `Object` | <span data-ttu-id="ae12f-181">界面和接口组并不以对象的形式直接表示在 JavaScript 中。</span><span class="sxs-lookup"><span data-stu-id="ae12f-181">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="ae12f-182">但是，接口表示为参数，并返回 Windows 运行时方法的返回类型。</span><span class="sxs-lookup"><span data-stu-id="ae12f-182">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="ae12f-183">Windows 运行时接口实例如下转换：</span><span class="sxs-lookup"><span data-stu-id="ae12f-183">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="ae12f-184">1. 如果有效运行时类，则该对象将表示为该类的实例。</span><span class="sxs-lookup"><span data-stu-id="ae12f-184">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="ae12f-185">2. 如果没有有效的运行时类，则将该对象表示为一个未命名运行时类的实例，该类实际实现该实例所知该实例所知称为实现的接口及其所需的接口。</span><span class="sxs-lookup"><span data-stu-id="ae12f-185">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="ae12f-186">对 JavaScript 值进行测试，确定它是运行时类还是接口的一个实例。</span><span class="sxs-lookup"><span data-stu-id="ae12f-186">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="ae12f-187">如果是，而原始的 Windows 运行时值实现接口，则会将该对象传递给 Windows 运行时。</span><span class="sxs-lookup"><span data-stu-id="ae12f-187">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span>  |  
| <span data-ttu-id="ae12f-188">运行时类</span><span class="sxs-lookup"><span data-stu-id="ae12f-188">Runtime classes</span></span> | `Object` | <span data-ttu-id="ae12f-189">Windows 运行时中的对象可以是实现一个或多个接口的运行时类的实例。</span><span class="sxs-lookup"><span data-stu-id="ae12f-189">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="ae12f-190">Windows 运行时对象以 JavaScript 表示为对象。</span><span class="sxs-lookup"><span data-stu-id="ae12f-190">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="ae12f-191">在所有类的实现接口上定义的方法、属性和事件的联合表示 JavaScript 对象的原型中的命名属性。</span><span class="sxs-lookup"><span data-stu-id="ae12f-191">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="ae12f-192">JavaScript 消费者可以直接访问类的任何成员。</span><span class="sxs-lookup"><span data-stu-id="ae12f-192">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="ae12f-193">Windows 运行时对象具有一个原型，该原型包含来自运行时类上所有实现接口的所有成员。</span><span class="sxs-lookup"><span data-stu-id="ae12f-193">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span>  |  
  
## <span data-ttu-id="ae12f-194">另请参阅</span><span class="sxs-lookup"><span data-stu-id="ae12f-194">See also</span></span>  

[<span data-ttu-id="ae12f-195">在 JavaScript 中使用 Windows 运行时</span><span class="sxs-lookup"><span data-stu-id="ae12f-195">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- links -->  

[WindowsRuntimeJavascript]: ./using-the-windows-runtime-in-javascript.md "在 JavaScript 中使用 Windows 运行时 |Microsoft 文档"  
