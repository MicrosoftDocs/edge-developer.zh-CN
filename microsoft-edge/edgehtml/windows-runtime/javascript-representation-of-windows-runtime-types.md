---
description: Windows 运行时类型 JavaScript 表示形式
title: Windows 运行时类型 JavaScript 表示形式
ms.custom: ''
ms.date: 10/30/2020
ms.prod: microsoft-edge
ms.technology: windows-integration
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
ROBOTS: NOINDEX,NOFOLLOW
ms.openlocfilehash: 18a288f95e7211794e3ceb88d700026dae359b06
ms.sourcegitcommit: 6cf12643e9959873f8b5d785fd6158eeab74f424
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 03/06/2021
ms.locfileid: "11399328"
---
# <a name="javascript-representation-of-windows-runtime-types"></a><span data-ttu-id="913fa-103">Windows 运行时类型 JavaScript 表示形式</span><span class="sxs-lookup"><span data-stu-id="913fa-103">JavaScript representation of Windows Runtime types</span></span>  

[!INCLUDE [deprecation-note](../includes/legacy-edge-note.md)]  

<span data-ttu-id="913fa-104">下表介绍了 JavaScript 表示 Windows 运行时类型的方式。</span><span class="sxs-lookup"><span data-stu-id="913fa-104">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="913fa-105">Windows 运行时功能不适用于在 Internet Explorer 中运行的应用。</span><span class="sxs-lookup"><span data-stu-id="913fa-105">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <a name="windows-runtime-types-in-javascript"></a><span data-ttu-id="913fa-106">JavaScript 中的 Windows 运行时类型</span><span class="sxs-lookup"><span data-stu-id="913fa-106">Windows Runtime types in JavaScript</span></span>  

| <span data-ttu-id="913fa-107">Windows 运行时类型</span><span class="sxs-lookup"><span data-stu-id="913fa-107">Windows Runtime type</span></span> | <span data-ttu-id="913fa-108">JavaScript 表示形式</span><span class="sxs-lookup"><span data-stu-id="913fa-108">JavaScript representation</span></span> | <span data-ttu-id="913fa-109">说明</span><span class="sxs-lookup"><span data-stu-id="913fa-109">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="913fa-110">Windows 运行时 `Uint8` 是一个无符号的 8 位整数，表示为 JavaScript 数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-110">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="913fa-111">JavaScript 值首先转换为 JavaScript 号码，然后 `ToUint32` 执行此过程。</span><span class="sxs-lookup"><span data-stu-id="913fa-111">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="913fa-112">如果 JavaScript 数字值超出 Uint8 的范围，则结果将应用模数 2^8。</span><span class="sxs-lookup"><span data-stu-id="913fa-112">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span>  |  
| `Int32` | `Number` | <span data-ttu-id="913fa-113">Windows 运行时 `Int32` 是一个 32 位有符号整数，表示为 JavaScript 数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-113">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="913fa-114">JavaScript 值首先转换为 JavaScript 号码，然后 `ToInt32` 执行此过程。</span><span class="sxs-lookup"><span data-stu-id="913fa-114">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="913fa-115">如果 JavaScript 数字值超出一个范围，则结果将 `Int32` 应用模数 2^16。</span><span class="sxs-lookup"><span data-stu-id="913fa-115">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span>  |  
| `Int64` | `Number` | <span data-ttu-id="913fa-116">Windows 运行时是一个有符号的 64 位整数，如果其位于 `Int64` \[-2^53， 2^53\] 范围内，则表示为标准数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-116">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="913fa-117">如果它超出此范围，则它表示为一个数字值，该数字值具有一个可维护完整 64 位整数数据的自定义备份存储。</span><span class="sxs-lookup"><span data-stu-id="913fa-117">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="913fa-118">这些自定义 Int64 数字值的所有数学运算都会导致值转换为区域 \[-2^53， 2^53\] 中的标准数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-118">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="913fa-119">如果值超出此范围，将引发类型错误。</span><span class="sxs-lookup"><span data-stu-id="913fa-119">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="913fa-120">此转换过程的例外情况是 ， 和操作，它们比较在传递两个表示的 64 位值时基于完整 `==` `===` `SameValue` `RelationalComparison` 64 位的参数。</span><span class="sxs-lookup"><span data-stu-id="913fa-120">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="913fa-121">如果任一参数是标准 JavaScript 数字，则这些操作还会在比较之前将参数转换为 JavaScript 数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-121">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="913fa-122">如果 JavaScript 值本身是 Int64 值，则直接分配该值;否则，对值应用 `ToInteger` 转换的结果将传递给 Windows 运行时。</span><span class="sxs-lookup"><span data-stu-id="913fa-122">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="913fa-123">如果类型强制失败，则返回异常。</span><span class="sxs-lookup"><span data-stu-id="913fa-123">If the type coercion fails, an exception is returned.</span></span>  |  
| `Uint64` | `Number` | <span data-ttu-id="913fa-124">Windows 运行时是一个无符号 64 位整数，如果位于 `Uint64` \[0， 2^53\] 范围内，则表示为标准数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-124">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="913fa-125">如果它超出此范围，则它表示为一个数字，该数字具有一个自定义支持存储，该存储可保留完整的 64 位无符号整数数据。</span><span class="sxs-lookup"><span data-stu-id="913fa-125">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="913fa-126">这些自定义 Uint64 值的所有数学运算都会导致值转换为范围 \[0， 2^53\] 中的标准数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-126">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="913fa-127">如果值超出此范围，将引发类型错误。</span><span class="sxs-lookup"><span data-stu-id="913fa-127">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="913fa-128">此转换过程的例外情况是 ， 和操作，它们比较在传递两个 64 位值时基于完整 `==` `===` `SameValue` `RelationalComparison` 64 位的参数。</span><span class="sxs-lookup"><span data-stu-id="913fa-128">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="913fa-129">如果任一参数是标准 JavaScript 数字，则这些操作还会在比较之前将参数转换为 JavaScript 数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-129">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="913fa-130">如果 JavaScript 值本身是 Uint64 值，则直接分配该值;否则，对值应用转换的结果，然后采用 `ToInteger` modulo 2^52，将传递给 Windows 运行时。</span><span class="sxs-lookup"><span data-stu-id="913fa-130">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="913fa-131">如果类型转换失败，则返回异常。</span><span class="sxs-lookup"><span data-stu-id="913fa-131">If the type conversion fails, an exception is returned.</span></span>  |  
| `Single` | `Number` | <span data-ttu-id="913fa-132">Windows 运行时是一个 32 位浮点数，通过选取最接近单精度值的双精度值来表示为 `Single` JavaScript 数字。</span><span class="sxs-lookup"><span data-stu-id="913fa-132">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="913fa-133">JavaScript 值转换为 JavaScript 数字，然后检查范围以确保该值可以使用单精度 32 位 IEEE 754 浮点数表示。</span><span class="sxs-lookup"><span data-stu-id="913fa-133">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="913fa-134">如果转换或区域检查失败，则返回封送错误。</span><span class="sxs-lookup"><span data-stu-id="913fa-134">If the conversion or range check fails, a marshaling error is returned.</span></span>  |  
| `Double` | `Number` | <span data-ttu-id="913fa-135">Windows 运行时 `Double` 是 64 位浮点数。</span><span class="sxs-lookup"><span data-stu-id="913fa-135">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="913fa-136">用于将 Windows 运行时 `Double` 转换为 JavaScript 号码。</span><span class="sxs-lookup"><span data-stu-id="913fa-136">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="913fa-137">如果转换失败，则返回封送错误。</span><span class="sxs-lookup"><span data-stu-id="913fa-137">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Boolean` | `Boolean` | <span data-ttu-id="913fa-138">Windows 运行时是一个 8 位值，其中零为，除零值外的任何值都表示 `Boolean` `false` 为 `true` `Boolean` JavaScript。</span><span class="sxs-lookup"><span data-stu-id="913fa-138">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="913fa-139">JavaScript 值首先被转换为 `Boolean` `ToBoolean` JavaScript。</span><span class="sxs-lookup"><span data-stu-id="913fa-139">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="913fa-140">这意味着声明为的 Windows 运行时函数可以在 JavaScript 中编写 `void func(BOOL);` 为 `obj.func("test");` 。</span><span class="sxs-lookup"><span data-stu-id="913fa-140">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="913fa-141">调用 Windows 运行时函数时， `BOOL` 参数传递为 `TRUE` 。</span><span class="sxs-lookup"><span data-stu-id="913fa-141">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="913fa-142">如果转换失败，则返回封送错误。</span><span class="sxs-lookup"><span data-stu-id="913fa-142">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Char16` | `String` | <span data-ttu-id="913fa-143">Windows 运行时 `Char16` 是一个 16 位 Unicode 字符，表示为包含单个字符的 JavaScript 字符串。</span><span class="sxs-lookup"><span data-stu-id="913fa-143">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="913fa-144">JavaScript 值将转换为 JavaScript 字符串，然后进行检查以确保该字符串只有 `ToString` 一个字符长。</span><span class="sxs-lookup"><span data-stu-id="913fa-144">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="913fa-145">然后，将单个字符作为值 `Char16` 传递。</span><span class="sxs-lookup"><span data-stu-id="913fa-145">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="913fa-146">如果转换或长度检查失败，则返回封送错误。</span><span class="sxs-lookup"><span data-stu-id="913fa-146">If the conversion or length check fails, a marshaling error is returned.</span></span>  |  
| `String` | `String` | <span data-ttu-id="913fa-147">Windows 运行时 `String` 是对象的不可变 `Char16` 序列。</span><span class="sxs-lookup"><span data-stu-id="913fa-147">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="913fa-148">字符串表示为 JavaScript `String` 对象。</span><span class="sxs-lookup"><span data-stu-id="913fa-148">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="913fa-149">Windows 运行时字符串没有不同的值和空 `null` 字符串 \(""\) 。</span><span class="sxs-lookup"><span data-stu-id="913fa-149">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="913fa-150">空字符串值转换为 JavaScript 空字符串。</span><span class="sxs-lookup"><span data-stu-id="913fa-150">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="913fa-151">**注意**：当 JavaScript 传递给需要字符串的 Windows 运行时参数时，它将生成字符串值 `null` "null"，而不是空字符串 `null` \(""\) 。</span><span class="sxs-lookup"><span data-stu-id="913fa-151">**NOTE**:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="913fa-152">传递给 Windows 运行时的字符串应始终实例化为空字符串。</span><span class="sxs-lookup"><span data-stu-id="913fa-152">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span>  |  
| `Enumeration` | `Object` | <span data-ttu-id="913fa-153">Windows 运行时 `Enumeration` 是一个 32 位整数 \(有符号或无符号\) ，具有一组关联的命名常量。</span><span class="sxs-lookup"><span data-stu-id="913fa-153">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="913fa-154">在 JavaScript 中，枚举表示为包含每个命名值的只读字段的对象。</span><span class="sxs-lookup"><span data-stu-id="913fa-154">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="913fa-155">枚举值将转换为 JavaScript 数字，如前面为 `Int32` `UInt32` 和 定义。</span><span class="sxs-lookup"><span data-stu-id="913fa-155">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="913fa-156">当 JavaScript 值转换为 Windows 运行时枚举时，它将进行转换、截断并检查范围，如前面所述。</span><span class="sxs-lookup"><span data-stu-id="913fa-156">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="913fa-157">但是，不会针对枚举中指定的已定义命名值检查生成的值。</span><span class="sxs-lookup"><span data-stu-id="913fa-157">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span>  |  
| `Structure` | `Object` | <span data-ttu-id="913fa-158">Windows 运行时 `Structure` 是命名数据字段的异类集合。</span><span class="sxs-lookup"><span data-stu-id="913fa-158">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="913fa-159">在 JavaScript 中，结构表示为 JavaScript 对象，该对象包含结构中每个字段的命名属性。</span><span class="sxs-lookup"><span data-stu-id="913fa-159">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="913fa-160">如果任何结构值的转换失败，则返回封送错误。</span><span class="sxs-lookup"><span data-stu-id="913fa-160">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="913fa-161">在 Windows 运行时结构中没有等效项的 JavaScript 对象中的字段将被忽略。</span><span class="sxs-lookup"><span data-stu-id="913fa-161">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="913fa-162">**注意**：无法使用关键字在 JavaScript 中实例化 Windows 运行时 `new` 结构。</span><span class="sxs-lookup"><span data-stu-id="913fa-162">**NOTE**:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span>  |  
| `Array` | `Array` | <span data-ttu-id="913fa-163">Windows 运行时 `Array` 将转换为 JavaScript 数组。</span><span class="sxs-lookup"><span data-stu-id="913fa-163">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="913fa-164">但是，Windows 运行时数组不可调整大小，因此无法执行某些 JavaScript 数组操作。</span><span class="sxs-lookup"><span data-stu-id="913fa-164">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="913fa-165">转换 `[[Class]]` 后的数组的内部属性未设置为"Array"，因为 ECMAScript 5 规范不允许这样做。</span><span class="sxs-lookup"><span data-stu-id="913fa-165">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="913fa-166">这意味着返回 `Array.isArray(v)` `false` 。</span><span class="sxs-lookup"><span data-stu-id="913fa-166">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="913fa-167">JavaScript 值转换为 Windows 运行时数组，如下所示：如果值为或，则 `null` 转换为 `undefined` 本机。 `null`</span><span class="sxs-lookup"><span data-stu-id="913fa-167">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="913fa-168">如果内部 `[[Class]]` 属性为"Array"，则它将复制到本机数组，并传递对此数组的引用。</span><span class="sxs-lookup"><span data-stu-id="913fa-168">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="913fa-169">如果它是已转换的数组，如前面所述，将传递基础本机数组。</span><span class="sxs-lookup"><span data-stu-id="913fa-169">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="913fa-170">**注意**：将 JavaScript 数组值传递给 Windows 运行时方法将导致数组的完整副本。</span><span class="sxs-lookup"><span data-stu-id="913fa-170">**NOTE**:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span>  |  
| <span data-ttu-id="913fa-171">委托 \(函数回调\) </span><span class="sxs-lookup"><span data-stu-id="913fa-171">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="913fa-172">Windows 运行时委托是对单个方法的引用。</span><span class="sxs-lookup"><span data-stu-id="913fa-172">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="913fa-173">Windows 运行时委托在 JavaScript 中表示为 JavaScript，它保证在正确的线程 `Function` 上调用。</span><span class="sxs-lookup"><span data-stu-id="913fa-173">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="913fa-174">调用 `Function` 时，参数将转换为等效的 Windows 运行时参数类型。</span><span class="sxs-lookup"><span data-stu-id="913fa-174">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="913fa-175">如果 JavaScript 参数少于委托参数 `in` ，则委托调用将失败。</span><span class="sxs-lookup"><span data-stu-id="913fa-175">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="913fa-176">如果 JavaScript 参数多于委托中的参数数，则 `in` 忽略额外的 JavaScript 参数。</span><span class="sxs-lookup"><span data-stu-id="913fa-176">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="913fa-177">调用委托后， `out` 参数将转换为 JavaScript 类型并返回。</span><span class="sxs-lookup"><span data-stu-id="913fa-177">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="913fa-178">如果将本机 JavaScript 函数对象转换为 Windows 运行时委托，它将包装在相应类型的 Windows 运行时委托中。</span><span class="sxs-lookup"><span data-stu-id="913fa-178">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="913fa-179">调用委托时， `in` 参数将转换为 JavaScript 类型。</span><span class="sxs-lookup"><span data-stu-id="913fa-179">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="913fa-180">调用委托后，返回值将转换为委托 `out` 的参数。</span><span class="sxs-lookup"><span data-stu-id="913fa-180">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span>  |  
| <span data-ttu-id="913fa-181">接口</span><span class="sxs-lookup"><span data-stu-id="913fa-181">Interface</span></span> | `Object` | <span data-ttu-id="913fa-182">接口和接口组不会直接在 JavaScript 中表示为对象。</span><span class="sxs-lookup"><span data-stu-id="913fa-182">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="913fa-183">但是，接口表示为 Windows 运行时方法的参数和返回类型。</span><span class="sxs-lookup"><span data-stu-id="913fa-183">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="913fa-184">Windows 运行时接口实例的转换方式如下：</span><span class="sxs-lookup"><span data-stu-id="913fa-184">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="913fa-185">1. 如果存在有效的运行时类，则表示该对象为该类的实例。</span><span class="sxs-lookup"><span data-stu-id="913fa-185">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="913fa-186">2. 如果没有有效的运行时类，则表示对象为未命名的运行时类的实例，该类完全实现该实例已知的要实现接口及其所需的接口。</span><span class="sxs-lookup"><span data-stu-id="913fa-186">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="913fa-187">测试 JavaScript 值以确定它是运行时类的实例还是接口。</span><span class="sxs-lookup"><span data-stu-id="913fa-187">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="913fa-188">如果是，并且原始 Windows 运行时值实现接口，则对象将传递给 Windows 运行时。</span><span class="sxs-lookup"><span data-stu-id="913fa-188">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span>  |  
| <span data-ttu-id="913fa-189">运行时类</span><span class="sxs-lookup"><span data-stu-id="913fa-189">Runtime classes</span></span> | `Object` | <span data-ttu-id="913fa-190">Windows 运行时中的对象可以是实现一个或多个接口的运行时类的实例。</span><span class="sxs-lookup"><span data-stu-id="913fa-190">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="913fa-191">Windows 运行时对象在 JavaScript 中表示为对象。</span><span class="sxs-lookup"><span data-stu-id="913fa-191">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="913fa-192">在类的所有实现接口上定义的方法、属性和事件联合表示 JavaScript 对象原型中的命名属性。</span><span class="sxs-lookup"><span data-stu-id="913fa-192">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="913fa-193">JavaScript 使用者可以直接访问该类的任何成员。</span><span class="sxs-lookup"><span data-stu-id="913fa-193">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="913fa-194">Windows 运行时对象具有一个原型，其中包含运行时类的所有实现接口中的所有成员。</span><span class="sxs-lookup"><span data-stu-id="913fa-194">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span>  |  
  
## <a name="see-also"></a><span data-ttu-id="913fa-195">另请参阅</span><span class="sxs-lookup"><span data-stu-id="913fa-195">See also</span></span>  

[<span data-ttu-id="913fa-196">在 JavaScript 中使用 Windows 运行时</span><span class="sxs-lookup"><span data-stu-id="913fa-196">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- links -->  

[WindowsRuntimeJavascript]: ./using-the-windows-runtime-in-javascript.md "在 JavaScript |Microsoft Docs"  
